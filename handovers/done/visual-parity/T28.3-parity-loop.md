# T28.3 — SPA ↔ mockup visual-parity loop (global handover)

Zero-context record of the T28 visual-parity flow (plan
`~/.claude/plans/the-new-web-version-graceful-sparrow.md`). The loop was **stopped by user
request after fixer batch 2** — read "Open items" before doing anything else.

## What happened (commits, in order)

| Commit | What |
|---|---|
| `ec907a4` | T28.0 — mdrender: whole-token `@@mention` spans, new `#tag` emission (`awiwi-mention`/`awiwi-tag` class contract), fence-aware substitution (fixed latent `@bug`-in-code-block bug). Backend 229→237 green. Handover `T28.0-tag-mention.md`. |
| `e55e2bb` | T28.1 — committed fixture tree `server/tests/visual/fixture/home/` reproducing the mockup sample content + `pairs.json` (12 pairs + unpaired recipe-audit). Handover `T28.1-fixture.md` (content map + per-page comparator exclusion lists). |
| `22494ed` | T28.2 — dep-free CDP screenshot harness `server/tests/visual/shoot.mjs` + README (chromium headless, full-page byte-stability capture, light-theme localStorage injection, ImageMagick RMSE + 1440×760 band slices). Handover `T28.2-harness.md`. |
| `7526d75` | T28.3 batch 1 — 19/21 findings fixed (F1–F21), 14 files in `server/frontend/src/**`. Includes real bugs: broken date ordinals ("13rd"), wrong journal-title format, search-chip active-state logic, Shiki light-theme background, doubled line spacing in text assets. |
| `7ea8010` | T28.3 batch 2 — 9/11 residuals fixed (G1–G11), 7 files. Breadcrumb day segment, dir-root descriptions, dir-month "Journal / July 2026" header, current-day highlight, image-frame width, fullscreen icon, text-asset file-info card. |

Full per-finding dispositions: `.claude/progress/orchestrator-t28.3.md` (gitignored; copy of the
disposition tables is effectively this handover's appendix — read it while it exists).

## How to rerun the loop

```sh
cd server/frontend && npm run build          # dist must be fresh (see Open item 1!)
cd server/tests/visual && node --experimental-websocket shoot.mjs \
  --out /tmp/<somewhere> [--only journal,todo]
```
Then compare band PNGs (mockup vs spa per page) against the exclusion lists in
`T28.1-fixture.md`. Mockup = ground truth.

## Final verification state (iteration-2 sweep on shots-iter4 + fixer batch-2 self-checks)

- **PARITY** (haiku-verified): todo, 404, asset-drawio, download, recipe-audit.
- **Fixed in batch 2, fixer-verified visually but NOT independently re-swept** (loop stopped):
  journal, journal-light, dir-root, dir-month, search, search-light, asset-image, asset-text.
- RMSE trend (iter1 → iter4, before batch 2): journal .106→.080, journal-light .104→.079,
  asset-text .086→.067, download .073→.067; rest flat-to-slightly-down.

## Open items (ordered)

1. **CRITICAL — served SPA is still the pre-parity bundle.** The repo-root `.gitignore:17`
   (`dist/`) ignores `server/frontend/dist` despite the committed-dist policy (ADR D20) and the
   frontend `.gitignore`'s "dist is committed" comment. The T28.3 builds' new hashed chunks are
   on disk but git-ignored/orphaned, and the tracked `dist/index.html` matches T26 (references
   `index-BiRkr-l8.js`, the pre-fix entry). Fix: decide `.gitignore` remedy (recommended: add
   `!server/frontend/dist/` negation), then `npm run build` and commit the fresh dist. Until
   then `:Awiwi serve` shows none of the T28.3 fixes.
2. Final independent verification sweep of the 8 batch-2 pages (one haiku comparator per pair on
   fresh shots) — the loop was stopped before this ran.
3. T28.4 close-out not run: `docs/architecture.md` §visual-regression-harness note,
   archive `handovers/visual-parity/` → `handovers/done/`, ADR if the `.gitignore`/dist decision
   warrants one.
4. G6 residual (documented, fixture-limited): with `/search?q=token` and no scopes selected the
   SPA honestly shows no active chip; the mockup illustrates one selected chip. Match would need
   a scoped fixture URL (e.g. `&scopes=journal`) in `pairs.json`, not a code change.

## Escalations (decided/deferred — do not "fix" silently)

- "Open in draw.io" button (mockup) intentionally NOT added — conflicts with DrawioView's
  documented decision not to leak diagram content to app.diagrams.net. Product/security call.
- Mockup factual errors found: download.html claims MIME `application/gzip` for `.tar.gz`
  (correct: `application/x-tar`); asset-image.html is the only asset mockup with a
  "← Back to…" link (treated as mockup inconsistency, not replicated).
- Data-gap exclusions (per user decision, styling-parity-only scope): file sizes/mtimes,
  dir-root "Recent files", todo filter chips/due badges, asset "Linked from", image caption text.
- Run-date drift: current-day highlight tracks real today (via `/api/meta`), mockup centers
  2026-07-14 — content diff only, style now matches.

## Gates at stop time

Backend `uv run pytest` 237 green (last run T28.1); frontend `npx vitest run` 123 green,
`npm run check` clean, `npm run build` clean (batch 2). All commits went through pre-commit
kb-detect legitimately (no `--no-verify` anywhere).

## Close-out (T28.4, 2026-07-15)

Final disposition of open items 1–4 above:

1. **CRITICAL .gitignore fix** (resolved, commit `2c9d5b5`): `!server/frontend/dist/` negation
   added to `.gitignore:19` (enforces ADR D20 committed-dist policy). Fresh `npm run build`
   bundle committed with new hashed entry (`index-But3saIX.js`, replacing pre-parity
   `index-BiRkr-l8.js`). `:Awiwi serve` now ships all T28.3 fixes.

2. **Final independent verification sweep** (completed): Fresh 8 screenshots of batch-2 pages
   via `shoot.mjs`, then 8 parallel haiku comparators (one per pair). **All 8 PARITY** within
   documented exclusions: journal, journal-light, dir-month, search, search-light, asset-text
   verified clean; dir-root and asset-image each raised one finding, both already documented
   (data-gap exclusions per user decision).

3. **G6 residual** (won't-fix, documented): scoping the fixture URL (`scope=journal`) would
   filter out assets/recipes result groups the mockup shows — worse diff than the inactive chip.
   Stays as documented limitation.

4. **Combined parity state**: 13/13 pages verified. RMSE final: journal .080, journal-light
   .079, dir-root .061, dir-month .071, search .089, search-light .089, asset-image .093,
   asset-text .068.

Knowledge base: `docs/architecture.md` §Toolchain added visual-regression-harness note; this
handover archived to `handovers/done/visual-parity/`. **T28 CLOSED.**
