# server-rewrite / T27 (S27.1) — delete legacy template stack + server.old

## Responsibility

Delete everything the T26 cutover (commit 25cc8fe) left dead on disk: the
Jinja template pages, the `/static` mount's files, the now-unregistered
`pages`/`assets`/`actions` routers, the Pygments-backed `render_file` path in
`mdrender.py`, and the entire Flask reference tree `server.old/`. Drop the
now-unused `jinja2`/`pygments`/`python-multipart` deps. Pure deletion +
dependency hygiene — no behavior change (confirmed nothing live imported any
of it before deleting).

Written for zero context. Boundary touched: the deletions below +
`server/pyproject.toml` + `server/uv.lock`. Also touched, per task
instructions, trivial dead-reference cleanup in a few source docstrings/
comments and doc files (see "Also touched" below) — kept minimal; the full
`docs/architecture.md` §Server rewrite is S27.2's job, not this unit's.

## Deleted

- `server/templates/` (7 Jinja files: `404.html`, `base.html.j2`,
  `dir.html.j2`, `journal.html.j2`, `non-journal.html.j2`,
  `search-content.html.j2`, `todo.html.j2`)
- `server/static/` (css/js/img, incl. vendored mermaid/nomnoml bundles)
- `server/src/awiwi/templating.py` (Jinja2 wiring, `theme_from_cookie`,
  `render`; its `get_home`/`is_localhost` re-export shim, dead since T23.1
  moved the real definitions to `httputil.py`)
- `server/src/awiwi/routers/pages.py`, `routers/assets.py`, `routers/
  actions.py` — confirmed dead from the T26 handover's "What T27 may
  delete" list; verified again before deleting that neither `app.py` nor
  `routers/__init__.py` import them (only `api`/`redirects` are imported/
  registered)
- The `render_file` function + its Pygments imports (`pygments.highlight`,
  `HtmlFormatter`, `Lexer`, `get_lexer_by_name`, `get_lexer_for_filename`,
  `ClassNotFound`) from `server/src/awiwi/mdrender.py`. `guess_language`
  (Shiki-id hint, used live by `docs.py`) and `LEXER_MAP` are unaffected —
  they don't touch Pygments.
- `server/tests/test_mdrender.py::TestRenderFile` (4 tests) and the
  `render_file` import. `TestGuessLanguage` untouched.
- The whole `server.old/` tree (Flask app, Jinja templates, vendored
  static assets, `nomnoml_extension.py`, `requirements.txt`, empty `auth`
  placeholder file, `.ruff_cache/`).

## Verified before deleting

- `grep` for `from awiwi.templating import` / `awiwi.templating` across
  `server/src` and `server/tests`: only `pages.py`, `assets.py`,
  `actions.py` (all three deleted in this unit) and `templating.py`
  itself. No other file imports from it. Nothing left dangling.
- `grep` for `render_file`/`pygments`/`Pygments`: only `mdrender.py` (the
  function itself) and `routers/pages.py` (deleted). `docs.py` only uses
  `guess_language`, never `render_file`.
- `routers/__init__.py` and `app.py` only import/register `api` and
  `redirects` — matches the T26 handover exactly.
- No dedicated `test_pages.py`/`test_assets.py`/`test_actions.py`/
  `test_templating.py` files existed (T26 already rewrote
  `test_acceptance.py` off the page-render assertions), so no test files
  needed deleting beyond `TestRenderFile`.

## `server/pyproject.toml` / deps

Removed `jinja2`, `pygments`, `python-multipart` from `dependencies`.
`uv sync` also transitively dropped `markupsafe` (jinja2's only dependent).
`server/uv.lock` updated (`-79` lines, 4 packages uninstalled: jinja2,
markupsafe, pygments, python-multipart).

## Also touched (trivial dead-reference fixes, in scope per task item 2/4)

- `server/src/awiwi/httputil.py` docstring: past-tense the `templating.py`
  re-export-shim mention (module is gone).
- `server/src/awiwi/docs.py` module docstring: past-tense the
  `routers/pages.py` "boundary note" (file is gone); dropped the dangling
  "flagged for a later cleanup subtask" sentence (this *is* that cleanup).
- `server/src/awiwi/schemas.py`: past-tense three `routers/pages.py`/
  `assets.py`/`actions.py` mentions (module docstring + two class
  docstrings).
- `server/tests/test_api.py`: fixed a stale comment referencing a
  nonexistent `docs.py:_guess_language` and Pygments lexer ambiguity — the
  actual hint (modeline `pgsql` → `sql` alias via `mdrender.LEXER_MAP`) is
  deterministic, not ambiguous.
- `server/tests/conftest.py`: two comments mentioning "pygments"/"lexer"
  reworded to "language hint" (fixture behavior unchanged, just describes
  it accurately post-Pygments-removal).
- `server/frontend/.gitignore`: removed the `dist` ignore line (T26 known
  wart — dist has been git-tracked since the T26 cutover commit, force-
  added with `git add -f`; future rebuilds no longer need `-f`). Verified
  `npm run build` still reproduces a byte-identical tree (no `git status`
  diff under `server/frontend/dist` after rebuilding).
- Root `.gitignore`: removed the dead `/server/static/npm` entry (the
  directory it referenced no longer exists).
- `docs/architecture.md`: minimal honest update, NOT the full §Server
  rewrite (that's S27.2) — updated the components table (Server/viewer row
  now describes the API+SPA shape and notes `server.old/` deletion;
  dropped the now-nonexistent "Legacy server" row), the "Server (viewer)"
  intro paragraph (`server.old/` past-tense), the `mdrender.py` module-map
  bullet (dropped the `render_file`/Pygments clause, added a T27 note),
  removed the `templating.py` bullet entirely, updated the `docs.py` and
  `httputil.py` bullets (past-tense `routers/pages.py`/`templating.py`),
  removed the `routers/pages.py`/`assets.py`/`actions.py` bullet (files no
  longer exist), and appended a short "T27 (S27.1) deleted..." pointer
  sentence so a reader lands on the truth even before S27.2's rewrite.
- `docs/glossary.md`: **viewer / server** entry no longer claims
  `server.old/` exists.

## Left for S27.2 (kb-curator's full rewrite)

Not touched — these are numerous, low-risk historical/provenance mentions
inside docstrings that explain *why* code is shaped a certain way (not
claims that the deleted files currently exist), better swept in one pass
by S27.2 rather than piecemeal here:

- `server/src/awiwi/docs.py` lines ~45, ~164, ~284, ~360 — "Verbatim copy
  of `routers/pages.py:_SECRET_RE`", "Dispatch mirrors
  `routers/pages.py:render_content_file`", "`routers/pages.py:journal`'s
  `/journal/{date_str}` route", "`routers/pages.py:index`/`dir_listing`" —
  all provenance notes, still accurate as history.
- `server/src/awiwi/routers/api.py` lines 3-4, 10, 58, 234 — several
  "legacy template routes (`routers/pages.py`, ...)" / "included *before*
  `routers/pages.py`" mentions; the ordering claim ("included before
  pages") is now moot since pages.py doesn't exist, but api.py's real
  ordering (`api` before `redirects`) is correct and unaffected.
- `server/src/awiwi/routers/redirects.py` line ~10 — one mention of
  `routers/pages.py` / `routers/assets.py` explaining why the redirect
  table exists.
- `docs/decisions.md` lines 103, 115, 255 — ADR entries referencing
  `server.old/`; append-only per project convention, left untouched
  (historically accurate at time of writing).
- `handovers/server-rewrite/*.md` (T13, T14, T15, T16, T23.1, T23.2,
  T23.3, T25.3, T25.4, T26) and `handovers/STATE.md` and
  `handovers/done/lua-port/server.md` reference `server.old/`,
  `templating.py`, `render_file`, etc. — these are archival records of
  completed subtasks, intentionally not rewritten (they document what was
  true when written).
- No stray `/static/*` URL references found anywhere under `docs/` (grepped
  clean).

## Gates

- `cd server && uv run pytest` → **229 passed** (234 − 5 deleted
  `render_file`/Pygments tests: 4 `TestRenderFile` + the removed import).
- `uv run ruff check .` → clean (`[]`).
- `uv run basedpyright` → 0 errors/warnings/notes.
- `cd server/frontend && npm run build` → success; `git status --porcelain
  server/frontend/dist` empty afterward (byte-identical rebuild, confirms
  the `.gitignore` fix doesn't create surprise untracked files).
- Pre-commit `kb-detect.sh`: `docs/architecture.md` staged alongside the
  `server/src/*.py` and `server/pyproject.toml` changes satisfies the
  mapped rules (server-code rule, pyproject rule, and the vim/lua/py
  catch-all all require *some* knowledge layer staged; `docs/
  architecture.md` covers all three). Not bypassed, no `DOCS_OK`.

## Deviations

None from the plan/handover boundary. One judgment call: fixed a handful
of dead-reference docstrings/comments beyond the strict "deletions +
pyproject.toml" boundary (listed under "Also touched" above) because task
item 2/4 explicitly asked to "fix trivial dead imports"/"fix trivially or
list them" — kept the list of what was fixed vs. deferred to S27.2 sharp so
the next unit knows exactly what's left.

## Status

status: done, updated 2026-07-14
commit: T27.1: delete legacy template stack + server.old
