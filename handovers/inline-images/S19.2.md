# S19.2 — integration probe: `awiwi.img` against real snacks.nvim

status: done (probe green, no code changes needed)
probe: scratchpad `img_probe.lua` (headless, `TERM=xterm-kitty`,
`--cmd 'set rtp+=/tmp/snacks.nvim'`, shallow clone of folke/snacks.nvim @ 2026-07-14)

## Verdict

`awiwi.img` works against the real backend unchanged. All assertions PASS:

- `require("snacks")` / `require("snacks.image")` load without `setup()`.
- **API-access choice confirmed**: the `require("snacks.image")` module table
  exposes `.config` (table) and `.doc.attach` (function), and is the *same
  table* as `_G.Snacks.image` — S19.1's module-table access path is correct,
  no `_G.Snacks` fallback needed.
- Real filetype pipeline: with `g:awiwi_home` set *before startup* (ftdetect
  bails at source time otherwise — known `--clean` artifact, see ledger
  28th run), a journal file opens with `filetype=awiwi`.
- `img.attach(buf)` → **true** on the real backend.
- `vim.treesitter.language.get_lang("awiwi") == "markdown"` after attach and
  `vim.treesitter.get_parser(buf)` succeeds (the load-bearing register call).
- Resolve chain: `image.config.resolve(file, "/assets/2024-03-05/pic.png")`
  → `<home>/assets/2024/03/05/pic.png`; relative src → nil (falls through to
  snacks' file-relative default, no pre-existing resolver in a clean session).

## Observations (not failures)

- `b:snacks_image_attached` stayed nil even though attach returned true —
  snacks evidently tracks attachment elsewhere (internal state), so don't
  rely on that buffer var; `img.attach`'s boolean is the contract.
- No pixel assertions possible headless; actual rendering is T21 (manual
  kitty dogfood). Probe env: `TERM=xterm-kitty` was sufficient for snacks'
  terminal gate headlessly.

## For S20.1

Contract unchanged from S19.1: one `require("awiwi.img").attach(buf)` call in
`ftplugin/awiwi.lua` after the syn attach (line ~185), no pcall, ignore return,
NOT inside the awiwiSynRepaint augroup.
